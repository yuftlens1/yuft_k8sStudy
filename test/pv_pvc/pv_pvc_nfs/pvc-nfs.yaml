apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-pv-nfs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: slow
#  volumeMode: Filesystem
